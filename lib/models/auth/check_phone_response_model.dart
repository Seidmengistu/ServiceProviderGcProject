class CheckPhoneResponseModel {
  bool? _isExist;

  CheckPhoneResponseModel(
    this._isExist,
  );
  //we have private variables
  bool? get isExist => _isExist; //we have private variables make it public

}
